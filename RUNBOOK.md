---
phase: validation_complete_awaiting_human_signoff
merge_train: cursor/modernisation-train-3359
verify_command: ant test
stop_conditions:
  - plan_unapproved
  - dual_run_drift
  - overlapping_paths
  - screen_java_before_characterisation
  - language_rewrite
path_ownership:
  s0_env: ["build.xml", ".github/**", "AGENTS.md", ".cursor/**"]
  s1_movement_tests: ["test/java/com/mojang/escape/movement/**"]
  s2_level_tests: ["test/java/com/mojang/escape/level/**"]
  s3_platform: ["build.xml", "src/com/mojang/escape/EscapeApplet.java", "src/com/mojang/escape/level/Level.java"]
  s4_packages: ["src/com/mojang/escape/**"]
  v1_presenter: ["src/com/mojang/escape/EscapeComponent.java"]
  v2_look: ["src/com/mojang/escape/InputHandler.java", "src/com/mojang/escape/EscapeComponent.java", "src/com/mojang/escape/Game.java", "src/com/mojang/escape/entities/Player.java"]
  v3_torches: ["src/com/mojang/escape/gui/Bitmap3D.java", "src/com/mojang/escape/level/block/TorchBlock.java"]
  v4_hud: ["src/com/mojang/escape/gui/Screen.java"]
human_gates_pending: []
plan_accepted: "2026-08-15 by repo owner (PR #3 merged; ADRs 0001+0002 accepted in chat)"
screen_java_unlocked: true
screen_java_unlock_reason: "S1 f8c8292 (movement) + S2 098ffd9 (level load) merged to train"
legacy_sha: ec62c8c25492df1e97d151e6b6a29cb5212c1617
characterisation_baseline_sha: 098ffd9482a3d2f7ab10d9d9c559c046f07da56e
---

# RUNBOOK — Prelude of the Chambered modernisation

Orchestrator-owned. Specialists do not edit this file except to append their artefact links when finishing a phase. **No production code in this phase.**

## Current phase

`validation_complete_awaiting_human_signoff` — all slices landed on the merge train `cursor/modernisation-train-3359` (single autonomous run; per-slice commits instead of per-slice PRs at the owner's request). Dual-run shows zero drift; evidence pack and per-slice recordings are attached to the train PR. The remaining human gate is merging the train into `master` (and later, flipping flag defaults).

## Artefacts

| Agent | Artefact | Status |
| --- | --- | --- |
| Discovery | `docs/architecture/LEGACY_MAP.md` | done |
| Plan & Design | `PLAN.md`, ADRs 0001/0002 | accepted 2026-08-15 |
| Test generation | S1 `f8c8292`, S2 `098ffd9` (tests only) | merged to train |
| Test execution | `evidence/reports/` (baseline 23/23, modern 46/46, zero drift) | done |
| Conversion | S3 `eaa6064` · V1 `047308a` · V2 `7e49175` · V3 `0dfabc9`+`8ec92e4` · V4 `d06ef97` · S4 `8b9f003` · dev flag `292e120` | merged to train |
| Validation | `evidence/EVIDENCE_PACK.md` + 4 recordings on the PR | done — go recommendation |

## North star (locked pending approval)

Modular monolith + Java 21. Not TypeScript. Not a new renderer.

Characterise movement and level load **before** `gui/Screen.java`.

## Merge train

- Integration branch to create after approval: `refactor/integration` (from `master`).
- One merge shepherd when allowlists collide.
- Humans merge; agents open draft PRs.

## Next spawn (only after approval)

1. One S0 agent — CI + `ant test` harness + `AGENTS.md` (prompt in `PLAN.md` §7).
2. Then S1 and S2 in parallel (tests only).
3. Then S3, V1–V4, and S4 last, as specified in `PLAN.md` (V2 and V4 wait for V1; V1∥V3 allowed; S4 package moves only after V4).

## Notes

- Combined discovery + plan in one docs PR because the scout already had the tree in-repo.
- `modernisation-framework.html` is the SDLC this runbook implements.
